#include "src_move.hpp"
#include <string>

/*
 * Estimation that a particular move_candidate was intended as a move by the
 * developer.
 */
float move_candidate::calc_move_distance(move_candidate original,
                                         move_candidate modified) {}
